function addUpTo(n){
    let total = 0;
    for(let i = 1; i <= n; i++){
        total += i;
    }
    return total;
}

/**
 * Here time complexity is O(n). Because the function operation is depend on the input. As is input, so is operation.
 */

